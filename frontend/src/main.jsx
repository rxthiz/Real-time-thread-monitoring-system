import React from "react";
import ReactDOM from "react-dom/client";

import ZoneMapDashboard from "./components/ZoneMapDashboard";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ZoneMapDashboard />
  </React.StrictMode>,
);
